<Project>

  <!--
    Makes the opt-in logging properties visible to the generator. Logging stays
    off unless MVVMGeneratorLogPath is set to a path whose directory exists.
  -->
  <ItemGroup>
    <CompilerVisibleProperty Include="MVVMGeneratorLogPath" />
    <CompilerVisibleProperty Include="MSBuildProjectDirectory" />
  </ItemGroup>

  <ItemGroup Condition="'$(UsingMicrosoftNETSdk)' != 'true'">
    <Analyzer Include="$(MSBuildThisFileDirectory)..\analyzers\dotnet\cs\MVVM.Generator.dll"
              Condition="Exists('$(MSBuildThisFileDirectory)..\analyzers\dotnet\cs\MVVM.Generator.dll')" />
  </ItemGroup>

</Project>
